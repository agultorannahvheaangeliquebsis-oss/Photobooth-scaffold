<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CloudinaryDotNet" Version="1.29.3" />
    <PackageReference Include="QRCoder" Version="1.6.0" />
    <!-- Windows-only (System.Drawing.Printing.PrintDocument); fine, since
         this whole solution already only runs on the Windows booth machine
         (WPF UI, PTP camera bridge, print spooler, LocalDB). -->
    <PackageReference Include="System.Drawing.Common" Version="8.0.10" />
  </ItemGroup>

</Project>
